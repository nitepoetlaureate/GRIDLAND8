# GRIDLAND — Agent Context

The authoritative working notes are in `/CLAUDE.md`. This file points to that and adds nothing else.

The only mandatory protocol is Mycelium logging via `python3 .mycelium/record.py` after every file creation or modification. See `/CLAUDE.md` for usage and conventions.
